      *    *===========================================================*
      *    * Area di comunicazione per modulo                "mpslct"  *
      *    *-----------------------------------------------------------*
       01  r.
      *        *-------------------------------------------------------*
      *        * Return status code                                    *
      *        *-------------------------------------------------------*
           05  r-rsc                      pic  x(02)                  .
      *        *-------------------------------------------------------*
      *        * Area di environment                                   *
      *        *-------------------------------------------------------*
           05  r-env.
      *            *---------------------------------------------------*
      *            * Codice azienda                                    *
      *            *---------------------------------------------------*
               10  r-env-cod-azi          pic  x(04)                  .
      *            *---------------------------------------------------*
      *            * Codice terminale                                  *
      *            *---------------------------------------------------*
               10  r-env-cod-ter          pic  x(08)                  .
      *            *---------------------------------------------------*
      *            * Codice utente                                     *
      *            *---------------------------------------------------*
               10  r-env-cod-ute          pic  x(08)                  .
      *            *---------------------------------------------------*
      *            * System date and time                              *
      *            *---------------------------------------------------*
               10  r-env-dat-tim          pic  9(15)                  .
      *        *-------------------------------------------------------*
      *        * Area di identificazione                               *
      *        *-------------------------------------------------------*
           05  r-ide.
      *            *---------------------------------------------------*
      *            * Sigla sistema applicativo                         *
      *            *---------------------------------------------------*
               10  r-ide-sis-app          pic  x(03)                  .
      *            *---------------------------------------------------*
      *            * Sigla area gestionale                             *
      *            *---------------------------------------------------*
               10  r-ide-are-ges          pic  x(03)                  .
      *            *---------------------------------------------------*
      *            * Sigla settore gestionale                          *
      *            *---------------------------------------------------*
               10  r-ide-set-ges          pic  x(03)                  .
      *            *---------------------------------------------------*
      *            * Sigla fase gestionale                             *
      *            *---------------------------------------------------*
               10  r-ide-fas-ges          pic  x(06)                  .
      *        *-------------------------------------------------------*
      *        * Area parametri fissi passati dal programma            *
      *        *-------------------------------------------------------*
           05  r-fix.
      *            *---------------------------------------------------*
      *            * Flags di tipo selezione                           *
      *            *                                                   *
      *            * 01 : (libero)                                     *
      *            * 02 : (libero)                                     *
      *            * 03 : (libero)                                     *
      *            * 04 : (libero)                                     *
      *            * 05 : (libero)                                     *
      *            * 06 : (libero)                                     *
      *            * 07 : (libero)                                     *
      *            * 08 : (libero)                                     *
      *            * 09 : (libero)                                     *
      *            * 10 : (libero)                                     *
      *            *---------------------------------------------------*
               10  r-fix-tip-sel          pic  x(10)                  .
      *            *---------------------------------------------------*
      *            * Codice stampante                                  *
      *            *---------------------------------------------------*
               10  r-fix-cod-stp          pic  x(08)                  .
      *            *---------------------------------------------------*
      *            * Tipo di stampa                                    *
      *            *---------------------------------------------------*
               10  r-fix-tip-sta          pic  x(01)                  .
      *            *---------------------------------------------------*
      *            * Codice modulo                                     *
      *            *---------------------------------------------------*
               10  r-fix-cod-mod          pic  x(08)                  .
      *            *---------------------------------------------------*
      *            * Tipo modulo                                       *
      *            *---------------------------------------------------*
               10  r-fix-tip-mod          pic  x(01)                  .
      *            *---------------------------------------------------*
      *            * Ampiezza linea di stampa in caratteri             *
      *            *---------------------------------------------------*
               10  r-fix-amp-lin          pic  9(03)                  .
      *            *---------------------------------------------------*
      *            * Top margin in linee                               *
      *            *---------------------------------------------------*
               10  r-fix-top-lin          pic  9(03)                  .
      *            *---------------------------------------------------*
      *            * Numero linee di stampa minimo                     *
      *            *---------------------------------------------------*
               10  r-fix-lin-min          pic  9(03)                  .
      *            *---------------------------------------------------*
      *            * Bottom margin in linee                            *
      *            *---------------------------------------------------*
               10  r-fix-bot-lin          pic  9(03)                  .
      *            *---------------------------------------------------*
      *            * Ampiezza caratteri                                *
      *            *---------------------------------------------------*
               10  r-fix-amp-car          pic  9(02)v9(02)            .
      *            *---------------------------------------------------*
      *            * Altezza interlinea                                *
      *            *---------------------------------------------------*
               10  r-fix-alt-int          pic  9(02)v9(02)            .
      *            *---------------------------------------------------*
      *            * Area riservata per espansioni future              *
      *            *---------------------------------------------------*
               10  r-fix-esp-fut          pic  x(99)                  .
      *            *---------------------------------------------------*
      *            * Area riservata per funzioni speciali              *
      *            *---------------------------------------------------*
               10  r-fix-fnz-spc          pic  x(99)                  .
